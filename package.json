{
  "name": "angular-appointment-scheduler",
  "version": "1.0.0",
  "description": "Enterprise-Grade Angular Appointment Scheduler with NestJS Backend",
  "author": "[Your Name]",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "[Your Repository URL - e.g., GitHub repo]"
  },
  "keywords": [
    "angular",
    "nestjs",
    "appointment",
    "scheduler",
    "calendar",
    "enterprise",
    "typescript",
    "material"
  ],
  "scripts": {
    "ng": "ng",
    "start:frontend": "ng serve -o",
    "build:frontend": "ng build",
    "watch:frontend": "ng build --watch --configuration development",
    "test:frontend": "ng test",
    "e2e:frontend": "ng e2e",
    "start:backend": "npm run start:dev -w=api",
    "build:backend": "npm run build -w=api",
    "start:dev": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "build": "npm run build:frontend && npm run build:backend",
    "test": "npm run test:frontend && npm run test:backend",
    "test:backend": "npm run test -w=api",
    "lint": "ng lint",
    "e2e": "ng e2e",
    "prepare": "husky install",
    "postinstall": "npm install -w=api"
  },
  "workspaces": [
    "api",
    "frontend"  <!-- Although frontend is technically the root dir, explicitly list if you plan to expand workspaces -->
  ],
  "private": true,
  "dependencies": {
    "@angular/animations": "^17.2.0",         <!-- Angular Core -->
    "@angular/cdk": "^17.2.0",               <!-- Angular CDK -->
    "@angular/common": "^17.2.0",              <!-- Angular Common -->
    "@angular/compiler": "^17.2.0",            <!-- Angular Compiler -->
    "@angular/core": "^17.2.0",                <!-- Angular Core -->
    "@angular/forms": "^17.2.0",               <!-- Angular Forms -->
    "@angular/material": "^17.2.0",            <!-- Angular Material -->
    "@angular/platform-browser": "^17.2.0",     <!-- Angular Platform Browser -->
    "@angular/platform-browser-dynamic": "^17.2.0", <!-- Angular Platform Browser Dynamic -->
    "@angular/router": "^17.2.0",              <!-- Angular Router -->
    "@nestjs/cache-manager": "^5.3.1",         <!-- NestJS Cache Manager (for caching example) -->
    "@nestjs/common": "^10.3.0",              <!-- NestJS Common -->
    "@nestjs/config": "^3.2.0",               <!-- NestJS Config Module (for env vars) -->
    "@nestjs/core": "^10.3.0",                <!-- NestJS Core -->
    "@nestjs/jwt": "^10.2.0",                <!-- NestJS JWT (for auth) -->
    "@nestjs/mapped-types": "*",               <!-- NestJS Mapped Types -->
    "@nestjs/platform-express": "^10.3.0",     <!-- NestJS Platform Express -->
    "@nestjs/swagger": "^7.3.0",             <!-- NestJS Swagger (for API docs) -->
    "@nestjs/testing": "^10.3.0",              <!-- NestJS Testing Module -->
    "@nestjs/typeorm": "^10.0.3",             <!-- NestJS TypeORM integration -->
    "@types/passport-jwt": "^3.0.13",         <!-- Types for Passport JWT -->
    "@types/rrule": "^2.6.5",                <!-- Types for rrule.js -->
    "cache-manager": "^5.4.0",                 <!-- Cache Manager Lib (NestJS Cache Module) -->
    "class-transformer": "^0.5.1",             <!-- Class Transformer (for ValidationPipe) -->
    "class-validator": "^0.14.1",               <!-- Class Validator (for ValidationPipe) -->
    "date-fns": "^3.3.1",                    <!-- date-fns date library -->
    "date-fns-timezone": "^2.1.0",            <!-- date-fns-timezone -->
    "reflect-metadata": "^0.1.3",             <!-- Required by NestJS and TypeORM -->
    "rrule": "^2.7.2",                       <!-- rrule.js - for recurring events -->
    "rxjs": "~7.8.0",                          <!-- RxJS -->
    "swagger-ui-express": "^5.0.0",           <!-- Swagger UI Express -->
    "tslib": "^2.3.0",                         <!-- TypeScript Lib -->
    "typeorm": "^0.3.20",                     <!-- TypeORM -->
    "zone.js": "~0.14.2"                        <!-- Zone.js -->
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^17.2.0",   <!-- Angular Devkit Build -->
    "@angular-eslint/builder": "^17.2.1",      <!-- Angular ESLint Builder -->
    "@angular-eslint/eslint-plugin": "^17.2.1", <!-- Angular ESLint Plugin -->
    "@angular-eslint/eslint-plugin-template": "^17.2.1", <!-- Angular ESLint Template Plugin -->
    "@angular-eslint/schematics": "^17.2.1",     <!-- Angular ESLint Schematics -->
    "@angular-eslint/template-parser": "^17.2.1", <!-- Angular ESLint Template Parser -->
    "@angular/cli": "^17.2.0",                 <!-- Angular CLI -->
    "@angular/compiler-cli": "^17.2.0",          <!-- Angular Compiler CLI -->
    "@types/jasmine": "~5.1.0",                <!-- Types for Jasmine -->
    "@types/node": "^20.11.19",                 <!-- Types for Node.js -->
    "@typescript-eslint/eslint-plugin": "^6.13.1", <!-- TypeScript ESLint Plugin -->
    "@typescript-eslint/parser": "^6.13.1",      <!-- TypeScript ESLint Parser -->
    "concurrently": "^8.2.2",                <!-- For running frontend/backend concurrently -->
    "eslint": "^8.55.0",                      <!-- ESLint -->
    "husky": "^9.0.11",                       <!-- Husky for Git hooks -->
    "jasmine-core": "~5.1.0",                  <!-- Jasmine Core -->
    "karma": "~6.4.0",                        <!-- Karma Test Runner -->
    "karma-chrome-launcher": "~3.2.0",         <!-- Karma Chrome Launcher -->
    "karma-coverage": "~2.2.0",                <!-- Karma Coverage -->
    "karma-jasmine": "~5.1.0",                 <!-- Karma Jasmine -->
    "karma-jasmine-html-reporter": "~2.1.0",    <!-- Karma Jasmine HTML Reporter -->
    "nest-cli": "^10.3.1",                    <!-- NestJS CLI -->
    "prettier": "^3.2.5",                    <!-- Prettier Code Formatter -->
    "source-map-support": "^0.5.21",          <!-- Source Map Support for NestJS -->
    "supertest": "^6.3.4",                   <!-- Supertest for backend E2E tests -->
    "ts-loader": "^9.5.1",                     <!-- TS Loader for Webpack (NestJS Build) -->
    "ts-node": "^10.9.2",                      <!-- TS Node for NestJS Dev -->
    "typescript": "~5.3.2"                     <!-- TypeScript Compiler -->
  }
}
